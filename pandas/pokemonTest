import pandas as pd 

df = pd.read_csv("data.csv", index_col='Name')

# SELECTION BY COLUMNS
# print(df['Name'].to_string())  # Single column
# print(df[['Name', 'Height']].to_string())  # Multiple columns

# SELECTION BY ROWS
# print(df.loc['Charizard' : 'Blastoise'])

# Search for a specific Pokemon
pokemon = input("Enter the name of the Pokemon: ")

try:
    print(df.loc[pokemon])
except KeyError:
    print(f"{pokemon} not found in the database.")